{
  "name": "ukonnect",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run dev --prefix Backend\" \"npm run dev --prefix UKConnect\"",
    "dev:backend": "npm run dev --prefix Backend",
    "dev:frontend": "npm run dev --prefix UKConnect",
    "build": "npm run build --prefix Backend && npm run build --prefix UKConnect",
    "install:all": "npm install && npm install --prefix Backend && npm install --prefix UKConnect"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

